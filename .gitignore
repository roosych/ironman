# ============================================
# Laravel 12 - Полный .gitignore
# ============================================

# ============================================
# СЕКРЕТЫ И КОНФИГУРАЦИЯ
# ============================================

# Файлы окружения с секретами (API ключи, пароли БД, токены)
.env
.env.*
!.env.example
# Исключаем .env.example, так как это шаблон без секретов

# Файлы аутентификации Composer (токены для приватных репозиториев)
auth.json
# Содержит токены доступа к приватным пакетам Composer

# ============================================
# ЗАВИСИМОСТИ И СБОРКИ
# ============================================

# Composer зависимости (устанавливаются через composer install)
/vendor
# Не коммитим зависимости, они восстанавливаются из composer.lock

# Node.js зависимости (устанавливаются через npm/yarn install)
/node_modules
# Не коммитим зависимости, они восстанавливаются из package-lock.json

# Vite сборки (генерируются при npm run build)
/public/build
/public/hot
# Сгенерированные файлы, пересобираются при деплое

# ============================================
# ЛАРАВЕЛЬ - ВРЕМЕННЫЕ И КЕШИРОВАННЫЕ ФАЙЛЫ
# ============================================

# Логи приложения
*.log
/storage/*.log
/storage/logs/*.log
# Логи содержат чувствительную информацию и быстро растут

# Кеш фреймворка
/bootstrap/cache/*
!/bootstrap/cache/.gitignore
# Кеш конфигурации и роутов, генерируется автоматически

# Кеш приложения
/storage/framework/cache/*
!/storage/framework/cache/.gitignore
# Кеш данных приложения, генерируется автоматически

# Сессии
/storage/framework/sessions/*
!/storage/framework/sessions/.gitignore
# Файлы сессий пользователей, не должны быть в Git

# Скомпилированные Blade шаблоны
/storage/framework/views/*
!/storage/framework/views/.gitignore
# Скомпилированные view, пересоздаются автоматически

# Laravel Pail (инструмент для просмотра логов)
/storage/pail
# Временные файлы инструмента просмотра логов

# Ключи шифрования
/storage/*.key
# Криптографические ключи приложения

# Символическая ссылка на storage (создается через php artisan storage:link)
/public/storage
# Символическая ссылка, создается на сервере

# ============================================
# ТЕСТИРОВАНИЕ
# ============================================

# PHPUnit кеш результатов
.phpunit.result.cache
/.phpunit.cache
# Кеш результатов тестов, ускоряет повторные запуски

# ============================================
# БАЗА ДАННЫХ
# ============================================

# SQLite база данных (локальная разработка)
*.sqlite
*.sqlite-journal
*.db
# Локальные БД не должны попадать в Git, каждый разработчик создает свою

# ============================================
# IDE И РЕДАКТОРЫ
# ============================================

# PhpStorm / IntelliJ IDEA
/.idea
*.iml
*.iws
*.ipr
# Настройки IDE, индивидуальны для каждого разработчика

# Visual Studio Code
/.vscode
!.vscode/settings.json.example
!.vscode/tasks.json.example
!.vscode/launch.json.example
!.vscode/extensions.json.example
# Настройки VSCode, можно добавить примеры настроек в Git

# Sublime Text
*.sublime-project
*.sublime-workspace
# Настройки Sublime Text

# NetBeans
nbproject/
# Настройки NetBeans

# Eclipse
.project
.classpath
.settings/
# Настройки Eclipse

# Fleet (JetBrains)
/.fleet
# Настройки Fleet IDE

# Zed
/.zed
# Настройки Zed редактора

# Nova (Laravel Nova IDE helper)
/.nova
# Кеш Laravel Nova

# ============================================
# AI ИНСТРУМЕНТЫ
# ============================================

# Claude AI
/.claude
.claude/
# Настройки и кеш Claude AI

# Cursor AI
/.cursor
.cursorrules
# Настройки Cursor AI (можно добавить .cursorrules.example в Git)

# GitHub Copilot
.copilot/
# Настройки GitHub Copilot

# ============================================
# WINDOWS СПЕЦИФИЧНЫЕ ФАЙЛЫ
# ============================================

# Thumbnails Windows
Thumbs.db
Thumbs.db:encryptable
ehthumbs.db
ehthumbs_vista.db
# Кеш миниатюр Windows Explorer

# Файлы блокировки Windows
*.stackdump
# Файлы дампа стека Windows

# Корзина Windows
$RECYCLE.BIN/
# Содержимое корзины Windows

# ============================================
# macOS СПЕЦИФИЧНЫЕ ФАЙЛЫ
# ============================================

# Системные файлы macOS
.DS_Store
.AppleDouble
.LSOverride
# Метаданные файловой системы macOS

# ============================================
# LINUX СПЕЦИФИЧНЫЕ ФАЙЛЫ
# ============================================

# Временные файлы Linux
*~
.directory
# Временные файлы редакторов Linux

# ============================================
# РАЗРАБОТКА И ИНСТРУМЕНТЫ
# ============================================

# PHP Actor (IDE помощник)
.phpactor.json
# Конфигурация PHP Actor

# Homestead (Laravel локальная среда)
Homestead.json
Homestead.yaml
# Конфигурация Homestead, индивидуальна для каждого разработчика

# Sail (Docker окружение)
docker-compose.override.yml
# Переопределения Docker Compose, индивидуальны

# ============================================
# ДОКУМЕНТАЦИЯ И ВРЕМЕННЫЕ ФАЙЛЫ
# ============================================

# Временные файлы
*.tmp
*.temp
*.swp
*.swo
*~
# Временные файлы редакторов

# ============================================
# ПРИМЕЧАНИЯ
# ============================================
# 
# ФАЙЛЫ, КОТОРЫЕ ДОЛЖНЫ БЫТЬ В GIT:
# - app/ (исходный код приложения)
# - routes/ (маршруты)
# - config/ (конфигурация без секретов)
# - resources/ (ресурсы: views, js, css)
# - database/migrations/ (миграции БД)
# - database/seeders/ (сидеры)
# - database/factories/ (фабрики)
# - tests/ (тесты)
# - composer.json, composer.lock (зависимости PHP)
# - package.json, package-lock.json (зависимости Node)
# - phpunit.xml (конфигурация тестов)
# - vite.config.js (конфигурация Vite)
# - artisan (CLI Laravel)
# - README.md, .gitignore (документация)
# - .env.example (шаблон окружения)
# 
# ФАЙЛЫ, КОТОРЫЕ НЕ ДОЛЖНЫ БЫТЬ В GIT:
# - .env (секреты)
# - vendor/ (зависимости)
# - node_modules/ (зависимости)
# - storage/logs/ (логи)
# - storage/framework/ (кеш)
# - public/build (сборки)
# - database/*.sqlite (локальные БД)
# - IDE настройки
# - AI инструменты настройки
# ============================================

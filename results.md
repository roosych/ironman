Ты — мой помощник по Laravel. Нужно сделать следующее:

1. Создать новую ветку Git, например `seed-athletes-results`.

2. Сделать два сидера:

   a) `AthleteProfileSeeder`:
      - Содержит список атлетов с полями:
        - `admin_full_name` — имя атлета для внутреннего использования
        - `role` — 'athlete'
        - `ironman_number` — номер атлета, может быть 0
        - `bio` — null
        - `social_links` — null
        - `user_id` — null по дефолту
        - `external_id` — уникальный идентификатор, сгенерировать через `Str::slug(admin_full_name, '_')`
      - Сидер должен использовать `firstOrCreate` чтобы повторный запуск не создавал дубликаты.
      - Выводить в консоль `info` с количеством созданных/синхронизированных профилей.

   b) `RaceResultsSeeder`:
      - Содержит результаты атлетов с полями:
        - `user_profile_id` — привязка через `external_id` профиля
        - `race_date`, `location`, `race_type`
        - `swim_time`, `t1_time`, `bike_time`, `t2_time`, `run_time`, `total_time`
        - `age_group`, `overall_position`, `age_group_position`
      - Сидер должен использовать `updateOrCreate` чтобы повторный запуск не создавал дубликаты.
      - Если профиль не найден по `external_id`, выводить предупреждение (`warn`) в консоль и пропускать результат.
      - Выводить в консоль `info` с количеством синхронизированных результатов.

3. Все сидеры должны быть идемпотентными и безопасными для повторного запуска.

4. Структура файлов и namespace должны соответствовать Laravel 11 и PSR-12 стандартам.

5. Выведи полностью готовый код обоих сидеров в одном ответе, чтобы можно было сразу вставить в проект и запустить.

6. После генерации предложи команду, которой можно будет их запустить (`php artisan db:seed --class=...`).

